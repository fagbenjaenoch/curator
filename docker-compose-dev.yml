services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"

  backend:
    build:
      context: ./app/backend
      target: dev
    depends_on:
      - redis
    environment:
      - REDIS_ADDR=redis:6379
      - PORT=3000
    ports:
      - "3000:3000"
    networks:
      - curator
    volumes:
      - ./app/backend:/app

  frontend:
    build:
      context: ./app/frontend
      target: dev
    depends_on:
      - backend
    ports:
      - "5173:5173"
    networks:
      - curator
    volumes:
      - ./app/frontend:/app
      - /app/node_modules

  python-service:
    build:
      context: ./app/python-service
      target: dev
    ports:
      - "8000:8000"
    volumes:
      - ./app/python-service:/app
    environment:
      - PYTHONUNBUFFERED=1
    networks:
      - curator

  db:
    image: postgres:latest
    restart: always
    environment:
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
      POSTGRES_DB: mydatabase
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:

networks:
  curator:
    driver: bridge
